// 화분
class Pot {
    constructor() {
        this.shape = 0;
        this.color = RED;

        this.stems = [];
    }

    display_back() {
        // 비즈보다 뒤에 있는 화분 뒷부분 그리기
        // (반)투명 화분의 정확한 표시를 위해 필요함
    }

    display_front() {
        // 비즈보다 앞에 있는 화분 앞부분 그리기
    }

    addStem() {
    }
}